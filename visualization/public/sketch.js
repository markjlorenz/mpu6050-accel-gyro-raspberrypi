// Generated by CoffeeScript 1.6.1
(function() {
  var SIZE, SPEED, canvas, processingInstance, sketchProc;

  SPEED = 2;

  SIZE = 200;

  sketchProc = function(P) {
    var xRotation, yRotation, zRotation;
    P.setup = function() {
      return P.size(600, 600, P.WEBGL);
    };
    xRotation = 5 * Math.PI / 180;
    yRotation = 5 * Math.PI / 180;
    zRotation = 5 * Math.PI / 180;
    P.draw = function() {
      P.background(224);
      P.lights();
      P.translate(P.width / 2.0, P.height / 2.0);
      P.rotateX(xRotation);
      P.rotateY(yRotation);
      P.rotateZ(zRotation);
      P.rotateX(P.mouseX / 10 * Math.PI / 180);
      P.rotateY(P.mouseY / 10 * Math.PI / 180);
      P.rotateZ((P.mouseX + P.mouseY) / 10 * Math.PI / 180);
      P.stroke(50, 50, 50);
      P.line(-SIZE, 0, 0, SIZE, 0, 0);
      P.line(0, -SIZE, 0, 0, SIZE, 0);
      P.line(0, 0, -SIZE, 0, 0, SIZE);
      P.noStroke();
      P.fill(166, 244, 130);
      return DATA.forEach(function(position) {
        P.translate.apply(P, position);
        return P.sphere(2);
      });
    };
    return P.keyPressed = function() {
      switch (P.key.toString()) {
        case "x":
          return xRotation += SPEED * Math.PI / 180;
        case "X":
          return xRotation -= SPEED * Math.PI / 180;
        case "y":
          return yRotation += SPEED * Math.PI / 180;
        case "Y":
          return yRotation -= SPEED * Math.PI / 180;
        case "z":
          return zRotation += SPEED * Math.PI / 180;
        case "Z":
          return zRotation -= SPEED * Math.PI / 180;
      }
    };
  };

  canvas = document.getElementById("canvas");

  processingInstance = new Processing(canvas, sketchProc);

}).call(this);
